# ============================================
# AresBot v3 环境变量配置
# 复制此文件为 .env 并填入实际值
# ============================================

# -------------------- 安全密钥 --------------------
# 生成命令: openssl rand -hex 32
ENCRYPTION_KEY=your_encryption_key_64_hex_chars

# -------------------- 服务端口 (对外暴露) --------------------
API_PORT=48000
MYSQL_PORT=43306
REDIS_PORT=46379

# -------------------- MySQL 配置 --------------------
MYSQL_ROOT_PASSWORD=root_password_change_me
MYSQL_DATABASE=aresbot
MYSQL_USER=aresbot
MYSQL_PASSWORD=aresbot_password_change_me

# -------------------- Redis 配置 --------------------
REDIS_PASSWORD=redis_password_change_me

# -------------------- Celery Worker 配置 --------------------
# 单个 Worker 的并发数（同时运行的策略数）
CELERY_CONCURRENCY=4

# Worker 副本数量
WORKER_REPLICAS=2

# 日志级别: debug, info, warning, error
CELERY_LOG_LEVEL=info

# ============================================
# 以下配置仅用于工作节点 (docker-compose.worker.yml)
# ============================================

# -------------------- 主节点连接配置 --------------------
# 主节点 IP 地址
MASTER_HOST=192.168.1.100

# 主节点 MySQL 端口 (与主节点 MYSQL_PORT 一致)
MASTER_MYSQL_PORT=43306

# 主节点 Redis 端口 (与主节点 REDIS_PORT 一致)
MASTER_REDIS_PORT=46379
